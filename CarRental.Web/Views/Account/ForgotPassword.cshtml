@model CarRental.Web.ViewModels.Account.ForgotPasswordViewModel
@{
    ViewData["Title"] = "Восстановление пароля";
    Layout = "_Layout";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-warning text-dark">
                    <h2 class="text-center mb-0">🔒 Восстановление пароля</h2>
                </div>
                <div class="card-body p-4">
                    <p class="text-muted mb-4">
                        Введите ваш email адрес, и мы вышлем инструкции для восстановления пароля.
                    </p>

                    <form method="post" asp-action="ForgotPassword">
                        @Html.AntiForgeryToken()
                        
                        @if (!ViewData.ModelState.IsValid)
                        {
                            <div class="alert alert-danger">
                                <ul class="mb-0">
                                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                                    {
                                        <li>@error.ErrorMessage</li>
                                    }
                                </ul>
                            </div>
                        }

                        <div class="mb-3">
                            <label for="Email" class="form-label fw-bold">Email адрес *</label>
                            <input type="email" id="Email" name="Email" class="form-control" 
                                   value="@Model?.Email" required /> <!-- ✅ Безопасная проверка -->
                            <span asp-validation-for="Email" class="text-danger small"></span>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-warning btn-lg">Отправить инструкции</button>
                        </div>

                        <div class="text-center mt-3">
                            <a href="@Url.Action("Login", "Account")" class="text-decoration-none">
                                ← Вернуться к входу
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}